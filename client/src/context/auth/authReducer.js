import {
  REGISTER_SUCCESS,
  REGISTER_FAIL,
  AUTH_ERROR,
  LOGIN_SUCCESS,
  LOGIN_FAIL,
  LOGOUT,
} from "../action";

// eslint-disable-next-line
export default (state, action) => {
  console.log(state, action);
  return;
};
